# HTML-Tags

A simple page that uses Javascript to display the avaialble HTML tags, their descriptions and some other attributes.

This was an attempt at some basic JS and CCS styling. The table is populated from the data in the `elements.json` file.


[Currenlty hosted on Netlify](https://htmltags.netlify.app/)
